# 代码静态分析配置
# 用于检查 Dart 代码中的错误、警告和规范问题

# 激活 Flutter 推荐规范
include: package:flutter_lints/flutter.yaml

linter:
  # 在此处自定义项目规范
  # 可以禁用某些规则或启用额外规则
  rules:
    # avoid_print: false  # 取消注释以禁用 avoid_print 规则
    # prefer_single_quotes: true  # 取消注释以启用 prefer_single_quotes 规则
